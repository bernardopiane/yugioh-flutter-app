import 'package:flutter/material.dart';

class CardSetPage extends StatelessWidget {
  const CardSetPage({Key? key, required this.setCode}) : super(key: key);
  final String setCode;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
